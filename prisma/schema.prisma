generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["referentialIntegrity"]
}

datasource db {
  provider             = "mysql"
  url                  = "mysql://078haajwqyx05zcdcnyl:pscale_pw_bKPZoGk7CFLxjGKEbeogfVcz5Xy0WWUx4rc6769OFUM@us-east.connect.psdb.cloud/nfp?sslaccept=strict"
  referentialIntegrity = "prisma"
}

model users {
  id            Int           @id @default(autoincrement())
  name          String
  profile_photo String?
  completions   completions[]
}

model challenges {
  id          Int           @id @default(autoincrement())
  name        String
  start_date  DateTime
  days        Int
  completions completions[]
}

model completions {
  id           Int         @id @default(autoincrement())
  user         users?      @relation(fields: [user_id], references: [id])
  user_id      Int
  challenge    challenges? @relation(fields: [challenge_id], references: [id])
  challenge_id Int
  day          Int

  @@index([user_id])
  @@index([challenge_id])
}
